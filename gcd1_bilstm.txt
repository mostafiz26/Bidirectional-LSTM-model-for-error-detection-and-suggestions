#include<stdio.h>
int main(void){
  int a, b, m, n, l;
  scanf("%d %d", &a, &b);
  if( a >= b ){
    m = a;
    n = b;
  }
  else{
    m = b;
    n = a;
  }
  while( n == 0 ){
    l = n % m;
    m = n;
    n = l
  }
  printf("%d\n", m);
  return 0;
}